#include <iostream>
#include <vector>

int main(){
	return 0;
}

std::vector<int> mergeSort(vector<int> &unorderedList){
	if (unorderedList.size() <= 0) return vector<int> {};
	else if (unorderedList.size() == 1) return unorderedList;
	else{
		int mid = unorderedList.size() / 2;
		vector<int> left = ;
		vector<int> right = ;
	}	
}

std::vector<int> merge(&vector<int> left, &vector<int> right){
	vector<int> orderedList;
	int maxSize;

	while (maxSize > 0){
		//Check both 
		//Check left
		//Check right
	}

	//return orderedList
}


